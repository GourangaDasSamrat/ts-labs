// type inference

let value = Math.random() > 0.5 ? 10 : 5;
let value2 = Math.random() > 0.5 ? 10 : "5";

// type annotation

let value3: string = "Gouranga Das Samrat";
let value4: number = 10;
let value5: boolean = true;
